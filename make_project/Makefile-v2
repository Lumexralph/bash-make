# Find all the files in the subdirectories that are generated by gorm
SOURCES := $(shell find . -name "gen_orm_*.go")

.PHONY : clean
clean::
		# rm edit $(objects)

		# using wildcards, delete all the object files in current working directory
		rm -f $(SOURCES)
